<div class="new-comment">
	<h3> New Comment  </h3>
</div>

<%= form_for Comment.new(:todo_item_id => item.id), :remote => true do |f| %>

	<%= f.hidden_field :todo_item_id %>

	<div class="field">
		<%= f.text_area :body %>
	</div>
	
	<%= f.submit %>
<% end %>